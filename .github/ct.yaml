# See https://github.com/helm/chart-testing#configuration
remote: origin
target-branch: main
chart-dirs:
  - charts
helm-extra-args: --timeout 600s
validate-maintainers: true
check-version-increment: false # Probably want to enable this in the future
# This is disabled for now to allow for easier development and testing of the chart