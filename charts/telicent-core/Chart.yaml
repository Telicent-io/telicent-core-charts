# Copyright (C) 2026 Telicent Limited

apiVersion: v2
name: telicent-core
description: |
  Telicent CORE Platform, for all essential services and applications for our
  secure data integration and knowledge graph platform.

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 1.2.1

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"

#  The chart's dependencies. These are other charts that this chart depends on
# The dependencies will be fetched and installed when the chart is installed.
dependencies:
  - name: admin-ui
    version: "^0.1.0"
    repository: file://./charts/admin-ui
    condition: global.enterprise
  - name: auth
    version: "^0.1.0"
    repository: file://./charts/auth
  - name: graph-ui
    version: "^0.1.0"
    repository: file://./charts/graph-ui
    condition: global.enterprise
  - name: query-ui
    version: "^0.1.0"
    repository: file://./charts/query-ui
  - name: search-ui
    version: "^0.1.0"
    repository: file://./charts/search-ui
    condition: global.enterprise
  - name: graph
    version: "^0.1.0"
    repository: file://./charts/graph
  - name: deletion-worker
    version: "^0.2.0"
    repository: file://./charts/deletion-worker
  - name: search
    version: "^0.2.0"
    repository: file://./charts/search
    condition: global.enterprise
  - name: search-projector
    version: "^0.2.0"
    repository: file://./charts/search-projector
    condition: global.enterprise
  - name: user-preferences
    version: "^0.2.0"
    repository: file://./charts/user-preferences
    condition: global.enterprise
  - name: traefik-proxy
    version: "^0.1.0"
    repository: file://./charts/traefik-proxy
  - name: document-pipeline
    version: "^0.1.0"
    repository: file://./charts/document-pipeline
    condition: global.enterprise, document-pipeline.enabled

icon: "https://avatars.githubusercontent.com/u/65304240?s=200&v=4"

# The name of the chart's maintainers.
maintainers:
  - name: telicent
    url: https://github.com/Telicent-IO
