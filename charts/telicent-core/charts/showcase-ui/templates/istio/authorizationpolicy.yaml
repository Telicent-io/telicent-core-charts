{{- /*
Copyright (C) 2026 Telicent Limited
*/}}
{{- if .Values.global.enterprise }}
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy

metadata:
  name: {{ include "showcase-ui.fullname" . }}-ingress
  namespace: {{ include "showcase-ui.namespace" . | quote }}
  labels: 
    {{- include "showcase-ui.labels" . | nindent 4 }}

spec:
  selector:
    matchLabels:
      {{- include "showcase-ui.selectorLabels" . | nindent 6 }}
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
          {{- include "showcase-ui.ingressPrincipal" . | nindent 8 }}
    to:
    - operation:
        methods: ["GET"]
{{- end }}
