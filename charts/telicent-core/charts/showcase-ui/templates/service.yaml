{{- /*
Copyright (C) 2026 Telicent Limited
*/}}

apiVersion: v1
kind: Service

metadata:
  name: {{ include "showcase-ui.serviceName" . }}
  namespace: {{ include "showcase-ui.namespace" . | quote }}
  labels:
    {{- include "showcase-ui.labels" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "showcase-ui.selectorLabels" . | nindent 4 }}
  ports:
  - name: http
    appProtocol: http
    protocol: TCP
    targetPort: http
    port: {{ .Values.service.port }}
