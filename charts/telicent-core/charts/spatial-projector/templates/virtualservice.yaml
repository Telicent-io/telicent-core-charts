{{- /*
Copyright (C) 2025 Telicent Limited
*/}}

{{- if .Values.global.istioVirtualServiceEnabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService

metadata:
  name: {{ include "spatial.fullname" . }}
  labels:
    {{- include "spatial.labels" . | nindent 4 }}

spec:
  gateways:
  - {{ .Values.global.istioNamespace }}/{{ .Values.global.istioGatewayName }}
  hosts:
  - {{ .Values.global.appHostDomain }}
  http:
  - match:
    - uri:
        prefix: /api/spatial
    route:
    - destination:
        host: {{ include "spatial.fullname" . }}
        port:
          number: {{ .Values.service.port }}
{{- end }}
