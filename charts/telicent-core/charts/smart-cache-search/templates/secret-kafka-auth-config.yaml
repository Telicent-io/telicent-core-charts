{{- if not .Values.projector.existingKafkaConfigSecret }}
apiVersion: v1
kind: Secret
metadata:
    name: {{ include "smart-cache-search.fullname" . }}-kafka-auth-config
    labels:
    {{- include "smart-cache-search.apiLabels" . | nindent 4 }}
type: Opaque
data:
    kafka-config.properties: {{ .Values.global.kafkaConfig | b64enc }}
{{- end }}