apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-pipeline.http-ingester.fullname" . }}
  labels:
    {{- include "document-pipeline.http-ingester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "document-pipeline.http-ingester.selectorLabels" . | nindent 4 }}
