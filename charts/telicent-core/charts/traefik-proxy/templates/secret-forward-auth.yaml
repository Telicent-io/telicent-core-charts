{{- /*
Copyright (C) 2025 Telicent Limited
*/}}
{{- if not .Values.forwardAuth.existingSecret }}
apiVersion: v1
kind: Secret
type: Opaque

metadata:
  name: {{ include "traefik-proxy.forwardAuthSecretName" . }}
  namespace: {{ include "traefik-proxy.namespace" . | quote }}
  labels:
    {{- include "traefik-proxy.labels" . | nindent 4 }}

data:
  header: {{ .Values.forwardAuth.header | b64enc | quote }}
{{- end }}
