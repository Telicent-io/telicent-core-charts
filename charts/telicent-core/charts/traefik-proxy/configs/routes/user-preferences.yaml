# Declare routers / services / middlewares
http:
  routers:
    user-preferences:
      rule: {{ printf "Host(`%s`) && PathPrefix(`/user-preferences`)" .Values.global.apiHostDomain }}
      service: user-preferences
      entryPoints:
        - web-api
      middleWares:
        - path-user-preferences
        - host-user-preferences
        - oauth2

  services:
    user-preferences:
      loadBalancer:
        servers:
          - url: http://{{ .Values.hosts.userPreferences | default (printf "%s.%s:8080" (include "traefik-proxy.routesService" (list . "user-preferences")) .Release.Namespace) }}

  middlewares:
    path-user-preferences:
      replacePathRegex:
        regex: "^/user-preferences/(.*)"
        replacement: "/$1"

    host-user-preferences:
      headers:
        customRequestHeaders:
          host: {{ .Values.hosts.userPreferences | default (printf "%s.%s:8080" (include "traefik-proxy.routesService" (list . "user-preferences")) .Release.Namespace) }}
