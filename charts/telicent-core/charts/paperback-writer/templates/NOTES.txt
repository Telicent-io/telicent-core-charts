{{- if and (not .Values.existingSecretName) (.Values.enabled) }}
WARNING: You are not using an existing Kubernetes Secret for credentials.

The default chart configuration will create a new Secret using values from `values.yaml`:
  - configuration.sparqlUser
  - configuration.sparqlPwd

**Storing sensitive credentials directly in values.yaml is NOT recommended for production deployments.**
- These values may be exposed in version control, CI/CD logs, or Helm history.
- For improved security, create a Kubernetes Secret manually and set `existingSecretName` to reference it.

Example:
  helm upgrade --install paperback-writer \
    --set existingSecretName=my-precreated-secret \
    ...

For more information, see:
  https://helm.sh/docs/chart_best_practices/security/
{{- else }}
Credentials are sourced from an existing Kubernetes Secret: {{ .Values.existingSecretName }}
No sensitive values are stored in values.yaml.
{{- end }}
