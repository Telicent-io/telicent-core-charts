{{- /*
Copyright (C) 2025 Telicent Limited
*/}}
{{- if and (not .Values.startup.groups.existingConfigMap) (.Values.startup.enabled) -}}
apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "auth.groupsConfigMapName" . }}
  namespace: {{ include "auth.namespace" . | quote }}
  labels:
    {{- include "auth.labels" . | nindent 4 }}

data:
  groups.yaml: |
    {{- if .Values.startup.groups.list }}
    groups:
      {{- toYaml .Values.startup.groups.list | nindent 4 }}
    {{- else }}
    groups: []
    {{- end }}
{{- end -}}
