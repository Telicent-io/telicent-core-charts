{{- /*
Copyright (C) 2025 Telicent Limited
*/}}

apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ include "user-preferences.fullname" . }}
  labels:
    {{- include "user-preferences.labels" . | nindent 4 }}

spec:
  replicas: {{ .Values.replicas }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
    {{- include "user-preferences.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
      {{- range $key, $value := .Values.annotations }}
        {{ $key }}: {{ $value | quote }}
      {{- end }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "user-preferences.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "user-preferences.serviceAccountName" . }}
      securityContext: {{- toYaml .Values.podSecurityContext | nindent 8 }}

      {{- $pullSecret := .Values.global.imagePullSecrets | default .Values.image.pullSecrets }}
      {{- with $pullSecret }}
      imagePullSecrets:
        {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
      - name: user-preferences
        image: {{ include "user-preferences.image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        resources: {{- toYaml .Values.resources | nindent 10 }}
        securityContext: {{- toYaml .Values.containerSecurityContext | nindent 10 }}

        env:
        {{- range $key, $value := .Values.extraEnvs }}
        - name: {{ $key }}
          value: {{ $value }}
        {{- end }}
        - name: MONGO_USER
          valueFrom:
            secretKeyRef:
              key: username
              name: {{ include "user-preferences.mongoSecretName" . }}
        - name: MONGO_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: {{ include "user-preferences.mongoSecretName" . }}
        envFrom:    
        - configMapRef:
            name: {{ include "user-preferences.envConfigMapName" . }}
        
        ports:
        - name: http
          protocol: TCP
          containerPort: 11111
        - name: metrics
          protocol: TCP
          containerPort: 9464

        readinessProbe:
          httpGet:
            path: /healthz
            port: 11111
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 60
          timeoutSeconds: 10
        livenessProbe:
          httpGet:
            path: /version-info
            port: 11111
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 60
          timeoutSeconds: 5
       
        volumeMounts:
        {{- if .Values.global.truststore.existingSecretName }}
        - name: truststore-volume
          mountPath: /app/config/truststore
        {{- end }}
        {{- if contains "tls=true" .Values.mongo.url }}
        - name: ca-certificate-volume
          mountPath: {{ .Values.mongo.cacertPath | default "/app/config/ca-certificate" }}
          readOnly: true
        {{- end }}
      {{- with .Values.extraContainers }}
        {{- toYaml . | nindent 6 }}
      {{- end }}

      volumes:
      {{- if .Values.global.truststore.existingSecretName }}
      - name: truststore-volume
        secret:
          secretName: {{ .Values.global.truststore.existingSecretName }}
          optional: true
      {{- end }}
      {{- if .Values.mongo.existingCaSecret }}
      - name: ca-certificate-volume
        secret:
          secretName: {{ .Values.mongo.existingCaSecret }}
      {{- end }}
     
