# Copyright (C) 2026 Telicent Limited
# Telicent Canonicals Ontology umbrella chart
#
# The pipeline components are not templated within this chart. They are provided by upstream
# Telicent dependency charts (see Chart.yaml), configured under their alias keys:
#   - canonicals-ontology-json-validation-mapper
#   - canonicals-ontology-mapper
#
# Unset values fall back to each dependency chart's own defaults (including container images),
# so each pinned dependency supplies its own tested image. Override under the relevant subchart
# key only if you need a specific configuration.
#
# The only resource templated by this chart itself is the ConfigMap carrying the canonical
# ontology JSON schema (templates/schema-configmap.yaml), sourced from
# files/canonical-ontology.schema.json.

# @key enabled Enable Canonicals Ontology chart
enabled: true

#
# @section Global Parameters
#
# @descStart
# Contains global parameters; these parameters are mirrored within the Telicent data umbrella chart
# Global values are automatically propagated to every dependency (subchart).
# Note: Only global parameters used within this chart will be listed below
# @descEnd
global:
  ## Current available global image parameters: imageRegistry and imagePullSecrets
  ## Please, note that this will override the image parameters
  # @key global.imageRegistry Global image registry
  imageRegistry: ""
  # E.g.
  # imagePullSecrets:
  #   - myRegistryKeySecretName
  # @key global.imagePullSecrets Global registry secret names as an array
  imagePullSecrets: []

  kafka:
    # @key global.kafka.bootstrapServers Comma separated list containing Kafka bootstrap servers
    bootstrapServers: "kafka-bootstrap.kafka.svc.cluster.local:9092"
    # @key global.kafka.existingConfigSecretName Name of an existing secret containing Kafka configuration (preferred over individual settings below for security)
    existingConfigSecretName: ""
    # @key global.kafka.username Username for Kafka authentication
    username: ""
    # @key global.kafka.password Password for Kafka authentication
    password: ""
    # @key global.kafka.protocol Protocol used for Kafka communication
    protocol: "SASL_SSL"
    # @key global.kafka.mechanism SASL mechanism used for Kafka authentication
    mechanism: "SCRAM-SHA-512"

#
# @section JSON Validation Mapper Parameters
#
# @descStart
# The canonical ontology JSON schema is shipped with this chart in
# `files/canonical-ontology.schema.json` and delivered to the mapper via a ConfigMap templated by
# this chart (see `templates/schema-configmap.yaml`). Subchart values cannot be templated, so the
# ConfigMap uses the fixed name referenced below; override this value to supply a schema from
# your own ConfigMap instead, in which case this chart's ConfigMap is not created.
# @descEnd
canonicals-ontology-json-validation-mapper:
  schema:
    # @key canonicals-ontology-json-validation-mapper.schema.existingConfigMapName Name of the ConfigMap containing the JSON validation schema; defaults to the ConfigMap created by this chart from `files/canonical-ontology.schema.json`
    existingConfigMapName: canonicals-ontology-schema
  configuration:
    # @key canonicals-ontology-json-validation-mapper.configuration.sourceTopic Topic the validation mapper reads raw canonical ontology messages from
    sourceTopic: "canonical.ontology.raw"
    # @key canonicals-ontology-json-validation-mapper.configuration.targetTopic Topic the validation mapper writes validated canonical ontology messages to
    targetTopic: "canonical.ontology.validated"
    # @key canonicals-ontology-json-validation-mapper.configuration.componentOf Pipeline this instance of the validation mapper belongs to
    componentOf: "canonicals-ontology"
