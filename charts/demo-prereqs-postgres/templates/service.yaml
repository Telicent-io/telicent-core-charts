apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-prereqs-postgres.fullname" . }}
  labels:
    {{- include "demo-prereqs-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "demo-prereqs-postgres.selectorLabels" . | nindent 4 }}
