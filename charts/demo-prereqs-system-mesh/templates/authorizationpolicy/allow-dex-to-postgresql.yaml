apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: {{ include "system-mesh.fullname" . }}-dex-to-pg
  labels:
  {{- include "system-mesh.labels" . | nindent 4 }}
spec:
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - {{ include "system-mesh.dexPrincipal" . }}
  selector:
    matchLabels:
      {{- include "system-mesh.postgresqlSelectors" . | nindent 6 }}
