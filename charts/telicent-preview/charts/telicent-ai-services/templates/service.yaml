{{- /*
Copyright (C) 2025 Telicent Limited
*/}}
{{- if .Values.enabled -}}
apiVersion: v1
kind: Service

metadata:
  name: {{ include "telicent-ai-services.serviceName" . }}
  namespace: {{ include "telicent-ai-services.namespace" . | quote }}
  labels:
    {{- include "telicent-ai-services.labels" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "telicent-ai-services.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      appProtocol: http
      protocol: TCP
      targetPort: http
      port: {{ .Values.service.port }}

    {{- if .Values.metrics.enabled }}
    - name: {{ .Values.metrics.service.name | default "metrics" }}
      appProtocol: http
      protocol: TCP
      targetPort: metrics
      port: {{ .Values.metrics.service.port }}
    {{- end }}

{{- end -}}
