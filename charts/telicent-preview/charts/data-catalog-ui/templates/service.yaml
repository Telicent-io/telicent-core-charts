apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-catalog-ui.fullname" . }}
  labels:
    {{- include "data-catalog-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-catalog-ui.selectorLabels" . | nindent 4 }}
